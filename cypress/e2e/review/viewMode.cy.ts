describe('review setlist in view mode', () => {
  // find the button
  // click the button
  // find the library sidebar and other elements to prove edit mode
  it('changes from view to edit mode when the edit button in the header is clicked', () => {

  });
});