# Songs Library

This page will have a scrollable main panel with all of the displayed songs. There will be a sidebar with a searchbar and filter tiles.

Song tiles will include only the title, but can expend if clicked. Once expanded, they can be minimized by tapping the bottom part which should have a shadow plus a hover bg.
