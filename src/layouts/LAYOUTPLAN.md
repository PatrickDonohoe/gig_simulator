# Layout Plan

Navbar will have tabs for "build setlist", "review setlists", and "analytics".

Initial plan is to have the navbar be the only common component between pages
with slots for individual sidebars and page content.
