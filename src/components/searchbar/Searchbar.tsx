
/**
 * Searchbar input and submit button.
 * Will require debouncing.
 * 3 character minimum with conditional formatting to indicate disabled/ready status. (able to search with disabled button and focus ring changing from orange => yellow => green as more characters are added).
 */

const Searchbar = () => {
  return (
    <div>Searchbar</div>
  )
}
export default Searchbar